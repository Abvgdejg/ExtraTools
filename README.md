# Extra Tools

1. TimeTools()
    - StartTimer(key)
    - ChechTimer(key)
    - StopTimer(key)

2. Logger(log_file, service)
    - info(text)
    - warning(text)
    - error(text)
    - critical(text)
    - debug(text)
    - special(text)

3. SpeedTest()